# We need to know where libRcpp.so and Rcpp.h reside
#PKG_CPPFLAGS = $(shell $(R_HOME)/bin/R --slave -e 'Rcpp:::CxxFlags()')
#PKG_LIBS = $(shell $(R_HOME)/bin/R --slave -e 'Rcpp:::LdFlags()')

PKG_CPPFLAGS = $(shell $(R_HOME)/bin/R.exe --slave -e 'Rcpp:::CxxFlags()')
PKG_LIBS = $(shell $(R_HOME)/bin/R.exe --slave -e 'Rcpp:::LdFlags()')

#PKG_CPPFLAGS = $(shell Rscript.exe -e "Rcpp:::CxxFlags()")
#PKG_LIBS = $(shell Rscript.exe -e "Rcpp:::LdFlags()")
