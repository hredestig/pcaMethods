# We need to know where libRcpp.so and Rcpp.h reside
#PKG_CPPFLAGS = $(shell Rscript -e 'Rcpp:::CxxFlags()')
#PKG_LIBS = $(shell env Rscript -e 'Rcpp:::LdFlags()')
PKG_CPPFLAGS = $(shell env R --slave -e 'Rcpp:::CxxFlags()')
PKG_LIBS = $(shell env R --slave -e 'Rcpp:::LdFlags()')
